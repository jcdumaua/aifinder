# AiFinder Phase 26HL — Duplicate Reason Protection Design

Static verification must require 22 unique allowlist entries and exactly one source fail branch per token.

Duplicates fail closed before any patch is applied.
