# AiFinder Phase 26IM — Helper-Only Diff Contract

The post-patch diff may contain only the exact helper replacement.

All request, parser, trace, environment, timeout, retry, redirect, and output logic outside the helper must remain byte-identical.
