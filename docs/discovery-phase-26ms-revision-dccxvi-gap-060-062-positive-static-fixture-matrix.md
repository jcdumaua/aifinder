# AiFinder Phase 26MS — Positive Static Fixture Matrix

Required positive fixtures:

- wrapper baseline mismatch before candidate begin marker;
- candidate begin marker followed by candidate `BASELINE_MISMATCH`;
- candidate begin and returned markers with integer status;
- existing 22 symbolic candidate reasons;
- existing fixed marker and approved diagnostic traces.
