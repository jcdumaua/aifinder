# AiFinder Phase 26ET — Sanitizer Fail-Closed Contract

The sanitizer must construct output from recognized allowlisted lines rather than redact arbitrary raw content.

Unknown output, malformed traces, duplicate traces, or denylisted patterns prevent clipboard copying and review-package generation.
