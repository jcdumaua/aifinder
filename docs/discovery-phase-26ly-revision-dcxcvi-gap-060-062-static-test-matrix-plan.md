# AiFinder Phase 26LY — Static Test Matrix Plan

Required synthetic tests:

- wrapper baseline mismatch before candidate invocation;
- candidate baseline mismatch after invocation marker;
- successful candidate return marker;
- duplicate marker rejection;
- malformed marker rejection;
- denylist non-regression;
- zero network activity.
