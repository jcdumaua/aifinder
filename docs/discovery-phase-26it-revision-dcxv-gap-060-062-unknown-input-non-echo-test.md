# AiFinder Phase 26IT — Unknown Input Non-Echo Test

For every rejected input, output must contain only the fixed marker and fixed unknown symbol.

The original rejected bytes must not appear in stdout or stderr.
