# AiFinder Phase 26ER — Execution Wrapper Requirements

The execution wrapper must verify all preconditions, invoke the candidate once, capture output locally, preserve the candidate exit status, and pass only allowlisted sanitized observations to the review package.

The wrapper must not alter the candidate or references.
