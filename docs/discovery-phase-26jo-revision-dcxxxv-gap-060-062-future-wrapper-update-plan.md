# AiFinder Phase 26JO — Future Wrapper Update Plan

Before any future candidate execution, the single-use wrapper must be updated to recognize the fixed marker and exact symbolic stop-reason allowlist.

The candidate must not be invoked under the prior sanitizer contract.
