# AiFinder Phase 26MU — Zero-Network Static Test Harness Plan

All reconciliation tests before fresh authorization must use synthetic local files and isolated subprocesses.

The harness must not invoke the wrapper, candidate, curl, Git fetch, Vercel, Supabase, or any external service.
