# AiFinder Phase 26KT — Single-Use Wrapper Update Plan

The future wrapper must embed the updated sanitizer contract, invoke the candidate exactly once, retain raw output locally with mode `0600`, and never retry.
