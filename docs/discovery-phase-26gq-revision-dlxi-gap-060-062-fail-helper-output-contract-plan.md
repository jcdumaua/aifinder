# AiFinder Phase 26GQ — Fail Helper Output Contract Plan

The fail helper may emit exactly:

1. `STOPPED_FAIL_CLOSED`
2. `stop_reason=<ALLOWLISTED_SYMBOL>`

No third line, value interpolation beyond the symbolic token, or diagnostic expansion is permitted.
