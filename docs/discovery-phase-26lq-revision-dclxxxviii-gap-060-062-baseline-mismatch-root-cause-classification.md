# AiFinder Phase 26LQ — Baseline-Mismatch Root-Cause Classification

Current static classification: `AMBIGUOUS_SHARED_SYMBOL`.

The immediate defect is diagnostic namespace overlap when both wrapper and candidate can emit the same symbol without a producer prefix.
