# AiFinder Phase 26MF — Shared Symbol Namespace Collision Record

Both wrapper and candidate can emit `BASELINE_MISMATCH`.

Disposition: preserve the candidate symbol and namespace the wrapper-local producer as `WRAPPER_BASELINE_MISMATCH`.
