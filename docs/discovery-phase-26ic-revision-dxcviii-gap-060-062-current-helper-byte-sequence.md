# AiFinder Phase 26IC — Current Helper Byte Sequence

```python
'fail() {\n  printf \'STOPPED_FAIL_CLOSED\\n\'\n  printf \'stop_reason=%s\\n\' "$1"\n  return 1\n}\n'
```

This exact sequence is the sole eligible replacement anchor.
