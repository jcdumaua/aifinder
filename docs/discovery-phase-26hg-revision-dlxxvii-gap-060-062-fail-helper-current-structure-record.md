# AiFinder Phase 26HG — Fail Helper Current Structure Record

The current fail helper emits a fixed fail-closed marker and a symbolic reason line before returning failure.

The remediation must preserve exactly two non-empty lines and exit status `1`.
