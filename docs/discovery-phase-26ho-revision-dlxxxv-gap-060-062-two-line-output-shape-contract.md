# AiFinder Phase 26HO — Two-Line Output Shape Contract

Every fail-helper path must emit exactly two non-empty lines:

1. fixed fail-closed marker;
2. one allowlisted symbolic reason.

No blank-value line or additional diagnostic line is permitted.
