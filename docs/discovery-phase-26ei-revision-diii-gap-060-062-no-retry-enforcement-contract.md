# AiFinder Phase 26EI — No-Retry Enforcement Contract

The future diagnostic operation may invoke the candidate exactly once and may permit no retry, fallback invocation, recursive invocation, or operator rerun under the same authorization.

Any failed attempt exhausts the authorization.
