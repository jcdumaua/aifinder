# AiFinder Phase 26HF — Dynamic Reason Rejection Contract

The fail helper must reject nonliteral, interpolated, empty, unknown, lowercase, whitespace-bearing, or non-allowlisted stop reasons.

Unknown reasons must not be echoed.
