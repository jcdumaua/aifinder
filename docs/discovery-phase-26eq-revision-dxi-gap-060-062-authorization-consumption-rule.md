# AiFinder Phase 26EQ — Authorization Consumption Rule

A fresh authorization is consumed when the candidate invocation begins, regardless of request success, pre-request failure after invocation, timeout, parser failure, or wrapper failure.

It cannot be reused.
