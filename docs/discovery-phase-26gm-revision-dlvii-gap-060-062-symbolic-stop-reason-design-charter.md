# AiFinder Phase 26GM — Symbolic Stop-Reason Design Charter

Any future remediation may expose only a reviewed symbolic stop-reason identifier from a strict allowlist.

It must not expose compared values, environment contents, paths beyond approved constants, URLs, selectors, or provider data.
