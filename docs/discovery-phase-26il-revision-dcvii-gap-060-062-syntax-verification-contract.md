# AiFinder Phase 26IL — Syntax Verification Contract

The exact expected transformed bytes passed `bash -n` in a restricted local temporary file.

Implementation must repeat `bash -n` after patching.
